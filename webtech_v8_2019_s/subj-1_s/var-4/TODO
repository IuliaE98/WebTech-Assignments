Să se modifice în locul marcat fișierul JS astfel încât să treacă testele

Puteți rula testele cu "npm test" în directorul main.

Modify the marked place in the JS file so that it passes the tests.

You can run the tests with "npm test" in the main directory.